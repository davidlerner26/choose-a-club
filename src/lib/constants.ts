// Valor interno usado como filtro de categoria "sem filtro" — nunca é
// exibido cru na tela (a UI sempre traduz pra "Tudo"/"All" etc via i18n),
// só serve como sentinela estável pra comparações de estado.
export const ALL_CATEGORIES = 'Tudo';
